// Este arquivo não é mais necessário - ToastContainer está dentro do ToastProvider
// Mantido apenas para compatibilidade, mas não é usado
export default function ToastContainer() {
  return null;
}
